services:
  authelia:
    image: authelia/authelia
    container_name: esir_authelia
    restart: unless-stopped
    volumes:
      - ./config:/config
    networks:
      - esir-network
    environment:
      - TZ=Europe/Paris

networks:
  esir-network:
    external: true