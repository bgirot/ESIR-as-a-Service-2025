services:
  dashy:
    image: lissy93/dashy:latest
    container_name: esir_dashboard
    restart: unless-stopped
    volumes:
      - ./public/conf.yml:/app/public/conf.yml
    networks:
      - esir-network
    
    # défaut, port 80

networks:
  esir-network:
    external: true