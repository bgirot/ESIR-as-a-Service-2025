services:
  dashboard:
    image: nginx:alpine
    container_name: esir_esir
    restart: always
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - esir-network

networks:
  esir-network:
    external: true