services:
  dashy:
    image: lissy93/dashy:latest
    container_name: esir_dashboard
    restart: unless-stopped
    volumes:
      - ./public/conf.yml:/app/user-data/conf.yml
      - ./icons:/app/public/local
    networks:
      - esir-network
    
    # défaut, port 8080

networks:
  esir-network:
    external: true