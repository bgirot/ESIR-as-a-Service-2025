services:
  dashboard:
    image: nginx:alpine
    container_name: esir_esir
    restart: unless-stopped
    volumes:
      - ./app:/usr/share/nginx/html:ro
    networks:
      - esir-network

networks:
  esir-network:
    external: true